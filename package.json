{
  "name": "fingerbot",
  "version": "1.0.0",
  "private": true,
  "description": "Press a Tuya Fingerbot Plus from a phone browser over Web Bluetooth. Static site, no server.",
  "type": "module",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'",
    "test:e2e": "node test/e2e.mjs",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
